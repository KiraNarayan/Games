import datetime
l = list(map(int,input("enter your DOB DD-MM-YYYY").split('-')))
bday = datetime.date(l[2],l[1],l[0])
today =  datetime.date.today()
diff = today - bday
print ("it has been {} beautifull days since u logged into earth".format(diff.days))
